These migrations were archived after the schema was unified around the
Business-based V1 model.

They are kept for reference only. Fresh local databases should bootstrap from
the active baseline migration in `packages/db/prisma/migrations/`.

If you have an older local database that was created from the archived
migration history, reset it before applying the new baseline.
